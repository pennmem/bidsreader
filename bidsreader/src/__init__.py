"""Support machinery for the readers in :mod:`bidsreader.readers`.

Nothing here is a reader. These are the shared pieces the readers are built
from: exception hierarchy, BIDS/validation helpers, unit handling, trial-type
filtering, and MNE-to-PTSA conversion. The commonly used names are re-exported
from the top-level ``bidsreader`` package.
"""
